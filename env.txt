conda deactivate

conda env remove -n 3d -y

conda create -n 3d python=3.11 -y

conda install -n 3d --override-channels `
  -c pytorch -c nvidia -c defaults `
  pytorch==2.3.0 torchvision==0.18.0 torchaudio==2.3.0 pytorch-cuda=12.1 -y

conda run -n 3d python -m pip install --upgrade pip

conda run -n 3d python -m pip install `
  numpy pillow tqdm pyyaml matplotlib tensorboard "setuptools<81" lpips gsplat pytest

conda run -n 3d python -c "import torch; print('torch:', torch.__version__); print('cuda:', torch.version.cuda); print('cuda available:', torch.cuda.is_available())"


winget install Microsoft.VisualStudio.2022.BuildTools --override "--wait --add Microsoft.VisualStudio.Workload.VCTools --includeRecommended"

where.exe cl
